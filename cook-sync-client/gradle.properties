org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true

# Human-facing app version (see app/build.gradle.kts). Bump these by hand when a release is
# worth calling out to users; the build number appended to versionName comes from CI instead
# and is not part of this decision.
versionMajor=1
versionMinor=0
